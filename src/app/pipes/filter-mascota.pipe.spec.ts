import { FilterMascotaPipe } from './filter-mascota.pipe';

describe('FilterMascotaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMascotaPipe();
    expect(pipe).toBeTruthy();
  });
});
